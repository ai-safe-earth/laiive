<!--
Title: Conventional Commits, same as the commits — `feat(frontend): …`, `fix: …`.
Base: `develop`, unless this is the release PR (`release: vX.Y.Z`, into `main`).
-->

## Why

<!-- The problem, not the diff. What was wrong, what it cost, what else was
     considered. The commit bodies are the reasoning; this is the summary of them. -->

## What changed

-

## Verified

<!-- What you actually ran, not what CI will run. e.g.
     - `cd services/retriever && uv run --no-sync python -m pytest -q -m "not integration"`
     - checked the pro gate and the composer in a browser at :8081 -->

-

## Notes for the reviewer

<!-- Migrations, env vars to add to the root `.env` and `.example.env`, anything
     needing an Aura/Supabase/Fly action, or a decision you want pushed back on.
     Delete if none. -->

Refs: #
